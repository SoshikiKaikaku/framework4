Bug {$post.id} status change to {$status[$post.status]}.

Bug id: {$post.id}
Previous Status: {$status[$post.oldstatus]}
Current Status: {$status[$post.status]}

Site: {$status_change.site}
Date: {$status_change.date}
